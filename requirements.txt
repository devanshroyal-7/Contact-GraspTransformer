numpy>=1.24,<2.0
torch>=2.0
wandb>=0.15
typing_extensions>=4.0

# Optional: required only when PTv3Wrapper is configured with
# cpe_mode='sparse3d' (3-D submanifold sparse conv CPE).
# Pick the wheel matching your CUDA, e.g. `pip install spconv-cu120`.
# spconv-cu120
